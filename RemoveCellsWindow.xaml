<Window x:Class="FilterV1.RemoveCellsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Remove Cell Pairs" Height="728" Width="808"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BackgroundBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Instructions -->
        <Border Grid.Row="0" Style="{StaticResource CardPanel}" Margin="0,0,0,15" Padding="15">
            <TextBlock TextWrapping="Wrap" Style="{StaticResource DescriptionText}">
                Define text patterns for removing rows. Rows where column 5 contains the first pattern 
                AND column 6 contains the second pattern will be removed.
            </TextBlock>
        </Border>

        <!-- Input Section -->
        <Border Grid.Row="1" Style="{StaticResource CardPanel}" Margin="0,0,0,15" Padding="15">
            <StackPanel Orientation="Horizontal">
                <TextBox x:Name="PasteTextBox" Width="400" Margin="0,0,15,0" 
                         Style="{StaticResource ModernTextBox}"
                         Text="Paste cell pairs here..." 
                         GotFocus="TextBox_GotFocus" 
                         LostFocus="TextBox_LostFocus" 
                         PreviewKeyDown="PasteTextBox_PreviewKeyDown"
                         ToolTip="Paste pairs from Excel or enter manually. Format: FirstPattern[Tab]SecondPattern"/>
                <Button x:Name="AddButton" Click="AddButton_Click" Content="Add Pairs" 
                        Style="{StaticResource ModernButton}" Width="100"/>
            </StackPanel>
        </Border>

        <!-- DataGrid for Cell Pairs -->
        <Border Grid.Row="2" Style="{StaticResource CardPanel}" Margin="0,0,0,15" Padding="15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Defined Pattern Pairs" 
                         Style="{StaticResource SubtitleText}" Margin="0,0,0,10"/>

                <DataGrid x:Name="CellPairsGrid" Grid.Row="1" 
                          Style="{StaticResource ModernDataGrid}"
                          AutoGenerateColumns="False" 
                          SelectionMode="Extended" 
                          CanUserAddRows="False" 
                          CanUserDeleteRows="False"
                          GridLinesVisibility="Horizontal"
                          HorizontalGridLinesBrush="{StaticResource BorderBrush}"
                          Background="White">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Column 5 Contains" Binding="{Binding FirstCell}" Width="*" IsReadOnly="True">
                            <DataGridTextColumn.HeaderStyle>
                                <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource {x:Type DataGridColumnHeader}}">
                                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.HeaderStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="Column 6 Contains" Binding="{Binding SecondCell}" Width="*" IsReadOnly="True">
                            <DataGridTextColumn.HeaderStyle>
                                <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource {x:Type DataGridColumnHeader}}">
                                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.HeaderStyle>
                        </DataGridTextColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <!-- Example Section -->
        <Border Grid.Row="3" Style="{StaticResource CardPanel}" Margin="0,0,0,15" Padding="15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Example:" Style="{StaticResource SubtitleText}" Margin="0,0,0,8"/>
                <TextBlock Grid.Row="1" TextWrapping="Wrap" Style="{StaticResource DescriptionText}">
                    Pattern "X2" + "K01" will remove rows where column 5 contains "X2" AND column 6 contains "K01"
                </TextBlock>
            </Grid>
        </Border>

        <!-- Button Section -->
        <Border Grid.Row="4" Background="Transparent" Padding="0,10,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="RemoveButton" Click="RemoveButton_Click" Content="Remove Selected" 
                        Style="{StaticResource DangerButton}" Width="140" Margin="0,0,10,0"/>
                <Button x:Name="ApplyButton" Click="ApplyButton_Click" Content="Apply Removal" 
                        Style="{StaticResource SuccessButton}" Width="120" Margin="0,0,10,0"/>
                <Button x:Name="CancelButton" Click="CancelButton_Click" Content="Cancel" 
                        Style="{StaticResource SecondaryButton}" Width="80"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>